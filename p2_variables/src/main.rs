fn main() {
    /*
     * integer default: i32
     * floating defailt: f64
     *
     *  // casting (changing type so it can be used with other types)
     * let int: i8 = 5;
     * let int_float: f32 = 5.0;
     * let int_change = int as f32;  // changed it to f32
     * let result = int_change + int_float;
     * println!("{}", result);
     *
     * // mutable vs immutabliity
     * mutable = changable
     * immutable = doesn't change
     *
     *
     *
     *
     *
     */
    println!("not much. here");
}
