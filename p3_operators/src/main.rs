fn main() {
    /*
     * Operators:
     *  +, -, /, *, %
     * 
     * Exponents:
     *  let squared: i32 = i32::pow(8, 2);
     *
     * Order of Operations (PEMDAS)
     * parentheses, exponents, mlutiplication, devision, addition, subtraction
     *
     * AND (&) = you need both to be true
     * OR (||) = you need 1 of them to be true
     *
     * Bitwise Operators:
     *  only used with bits
     *  do more research, I don't see me using this much at my level
     *
     */
    println!("Not much here since its fairly global in programming");
}
